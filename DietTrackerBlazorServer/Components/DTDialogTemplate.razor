@typeparam TItem


<MudDialog ClassContent="@($"border-t-2 {GetBorderColour()}")">

    <DialogContent>
        @{
            if (ItemFields != null)
            {
                if (Mode == DialogMode.Add)
                {
                    @ItemFields(new DialogContext(){Item = this.Item, Delete = false})
                }
                else if (Mode == DialogMode.Edit)
                {
                    @ItemFields(new DialogContext(){Item = this.Item, Delete = false})
                }
                else if (Mode == DialogMode.Delete)
                {
                    @ItemFields(new DialogContext(){Item = this.Item, Delete = true})
                }
            }

        }
    </DialogContent>
    <DialogActions>
        @if (Mode == DialogMode.Add)
        {
            <MudButton Color="Color.Success" OnClick="() => _Instance.Close()">Add</MudButton>
        }
        else if (Mode == DialogMode.Edit)
        {
            <MudButton Color="Color.Warning" OnClick="() => _Instance.Close()">Save changes</MudButton>
        }
        else if (Mode == DialogMode.Delete)
        {
            <MudButton Color="Color.Error" OnClick="() => _Instance.Close()">Delete</MudButton>
        }

        <MudButton OnClick="@(() => _Instance.Cancel())">Cancel</MudButton>

    </DialogActions>
</MudDialog>

