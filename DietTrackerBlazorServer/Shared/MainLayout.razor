@using Microsoft.AspNetCore.Identity
@inherits LayoutComponentBase


<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>

    <MudAppBar>
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        Health Tracker
    </MudAppBar>

    <MudDrawer @bind-Open="@_drawerOpen">
        <MudNavMenu>
            <MudNavLink Href="/">Dashboard</MudNavLink>
            <MudNavLink Href="/HealthMetrics">Edit metrics</MudNavLink>
            <MudNavGroup Title="Stats" Expanded="true">
                <MudNavLink Href="/Stats">Stats</MudNavLink>
                <MudNavLink Href="/HealthDataPoints">Data points</MudNavLink>
            </MudNavGroup>
            <MudNavLink Href="/about">About</MudNavLink>
        </MudNavMenu>
    </MudDrawer>

    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.Medium">
            @Body
            <MudSelect T="string" Label="Character" Variant="Variant.Filled" AnchorOrigin="Origin.CenterRight">
                <MudSelectItem Value="@("A")" />
                <MudSelectItem Value="@("B")" />
                <MudSelectItem Value="@("C")" />
            </MudSelect>
        </MudContainer>
    </MudMainContent>
</MudLayout>
